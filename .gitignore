# SNES build intermediates (regenerated by the toolchain).
# NOTE: data.asm and hdr.asm are hand-written SOURCE (root) and are kept.
src/*.asm
src/*.ps
*.obj
res/soundbank.asm
res/soundbank.h
res/soundbank.bnk
linkfile
.pvsneslib_home

# ROM output (build with `make`)
*.sfc
*.smc
*.sym

# Asset previews (regenerated; large, not needed to build)
res/*_preview.png
res/*_preview.wav
res/*_preview.mp3
preview_*.png

# Unused old tracker-music experiment
*.xm

# Python / OS
__pycache__/
*.pyc
.DS_Store
